# Test / CI dependencies.
# Lightweight subset: tests use fakeredis + in-memory SQLite instead of real
# Redis/Postgres, so no audio libs or database servers are needed on CI.
pytest==9.0.3
pytest-cov==7.1.0
anthropic==0.101.0
requests==2.32.5
sqlalchemy==2.0.36
psycopg2-binary==2.9.12   # postgres dialect is imported eagerly by create_engine
redis==7.4.0
fakeredis==2.36.1
python-dotenv==1.2.2
